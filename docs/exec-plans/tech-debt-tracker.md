# Tech Debt Tracker

Accepted residuals, each with a revisit trigger. A row is OPEN or CLOSED
in its last cell; the session-start hook counts OPEN rows. Numbers are
never reused.

| # | Description | Severity | Added | Source | Revisit trigger | Status |
|---|-------------|----------|-------|--------|-----------------|--------|
| 1 | Phase 0 commits (through 9f63d34) landed directly on main with `git add -A` staging and no reviewer gate; the harness that forbids both arrived after them. The Phase 0 gate (exec plan `phase-0-spike.md`) reviews that whole range retroactively. | Medium | 2026-09-06 | harness adoption | Closes when the Phase 0 gate APPROVEs the range or files its findings as rows here | OPEN |
| 2 | The staging-block hook (`.claude/hooks/block-git-add-all.sh` on `harness/enforcement`) is a text scanner, not a shell, and Dean froze further hardening on 2026-09-06. Measured gaps at 7f0dba3 (adversarial rounds 1-3, 250+ payloads): a `git add` nested inside `$(...)` or backticks; `${PWD}`/`${HOME}` forms (the brace splitter cuts them); a trailing slash on `$PWD`; `src/..` parent traversal; `--u`/`--up` abbreviations; `xargs -I{} git add {}`; a blanket argument hidden in a shell variable or built by eval/printf/`${IFS}`; interpreter wrappers; git aliases; a literal absolute toplevel path. The hook is a backstop; the discipline is the session's. | Low | 2026-09-06 | harness gate rounds 1-3 | If a blind stage ever lands in a commit again, or at Phase 1 when the gate rules return | OPEN |
| 3 | `RunState` gained `pendingPicks` (act-1 wave) under an unchanged `v: 1`. Correct today: no `persist.ts`, no localStorage code, no save ever written, `SAVE_VERSION` has no consumer. The adversarial seat's position: bumping for a shape nobody stored only spends a number. | Low | 2026-09-08 | act-1 wave gate | Closed 2026-09-08: persist.ts (Phase 1) refuses any version but SAVE_VERSION and any shape but RUN_STATE_KEYS; v1 to v2 (tuning wave) and v2 to v3 (effects wave) were bumped with drop notes as this row asked | CLOSED |
| 4 | A run cannot be abandoned from the fight screen. A hand-edited save that passes persist's shape check but yields a non-throwing broken state (e.g. `encounter.enemy: {}`) renders NaN HP and never ends; the only exit is clearing site data. Not reachable by the app's own writes (Phase 1 gate, adversarial seat). | Low | 2026-09-08 | Phase 1 gate | Closed 2026-09-08 by the feel wave: the title screen's New run asks, then abandons and replaces the save; Menu is reachable from every run screen | CLOSED |
| 5 | The boss's `lockTiles` picks from `playableIndices` on the pre-refill grid, which still holds the tiles just played, and `refill` then overwrites those with plain tiles: the lock is lost on most boss specials. Measured by the gravity round (2026-09-08) on 120 seeds x 2 bots: exactly 3 tiles locked in 32 of 137 specials on the gravity branch, 22 of 136 on main. Pre-dates gravity. | Medium | 2026-09-08 | gravity gate | Closed 2026-09-08 by the tuning wave (PR #19): lockTiles excludes the current selection; 30-seed test binds it | CLOSED |
| 6 | The mediocre bot's `offerScore` (scripts/lib/bots.ts) counts usable effects, so five of the eight mythics score no higher than a two-effect common and the bot passed on 43% of the mythics it was offered (adversarial round, PR #31, 500 runs). The ROADMAP's "22% of mediocre runs pick a mythic" is a measurement of that bot, not of the items' pull. Sim numbers stay honest; the plan's claim "mythics score high by construction" was false. | Low | 2026-09-08 | mythic gate | Closed 2026-09-08 by the effects wave engine PR (#39): offerScore breaks ties by rarity and reads every new verb | CLOSED |
| 7 | After the effects wave engine PR (#39) the mediocre bot wins 48.4% with the unchanged 72 items, above its 20-40% band. Measured: the offer rule (at most two commons, Dean's question 2) adds ~3.5 points, the bot's new rarity tiebreak ~8 (39.8% with the tiebreak off, 36.4% with both off). No content changed; the plan's "criteria pass after the engine PR" acceptance is not met. | Medium | 2026-09-08 | effects wave engine PR | Closed 2026-09-08 by PR #41: curve D (act 2 and 3 harder) with 112 items, mediocre 30.8%, greedy 66.0%, all criteria PASS | CLOSED |
| 8 | The enrage clock (variety wave step 1, PR #61) guarantees a fight ends only while the enemy attacks: it grows the enemy's damage, and a stun eats the attack. Two stun items whose conditions fire on alternating turns (the gate's repro: Paralytic plus Clockwork, both on even turns, against Abyssal Mat with `enrageAfter: 3`) lock the enemy permanently; 400 word turns, one hit landed, enemy damage at 412, the fight still running. Not reached by any sim bot over 500 seeds; a player would have to hold both and choose to stall. | Low | 2026-09-09 | variety wave step 1 gate | Closed 2026-09-09 by the modes PR (#66): past tuning.enrageAfter a stunned enemy attacks through the stun, so a stun lock ends with the clock; Endless is what surfaced it, since a mode with no last slot gives the lock all the time it needs | CLOSED |
| 9 | The four RNG-gated selection guards in PR #65 (`goldTiles` and `crackTiles` never re-mark or pick an already-played tile; a cracked tile's crumble is computed after the lock tick) are covered by the statistical cracks probe (839 turns, recracked=0, lockedCracked=0, orderViolations=0) rather than by deterministic single-seed unit assertions. A regression in the selection RNG would show as a non-zero counter over many turns, not as a failed fixed-seed case. | Low | 2026-09-10 | modes PR (#66), folding in #65 | If a future change touches `goldTiles`/`crackTiles` selection, bind these guards with seed-fixed unit tests | OPEN |
| 10 | The gambler cell wins above the "cells within 10 of Amoeba" band on the greedy bot: 95.4% no-curse vs balanced 84.4% (a 11.0pp gap), 95.0% vs 83.4% with curses. Pre-dates the curses wave (PR #76): the gap is 11.0pp in the no-curse baseline, so curses did not create it (they shift gambler greedy by -0.4pp). Balanced, the judged cell, passes every exit criterion. Measured 2026-09-10, 500 runs/bot, seeds 0..499. | Low | 2026-09-10 | curses wave (PR #76) | Retune cell balance (gambler's 50%-on-6+/half-on-3 quirk gives the greedy bot a high ceiling) in a dedicated cell-tuning wave, or when Dean asks | CLOSED 2026-09-11 by PR #85: Spore 6+/+50% -> 7+/+30% (greedy 95.4 -> 89.2, under the guard); Diatom, which had drifted to +12 mediocre, folded in (HP 115 -> 108, reduce 2 -> 1, mediocre 35 -> 27). 500 runs/bot: every cell mediocre within ~8 of Amoeba, greedy < 90 everywhere. |
| 11 | `turnStart` checks enemy death (after enemyTraitsTick and poison) before player death, so if an onTurnStart `damageEnemy` boon kills the enemy on the same turn a self-damage source (`apex`/`symbiont`, or a curse's onTurnStart `damagePlayer`) brings the player to 0, the encounter ends (or the final boss is won) at 0 HP and the player's death is deferred to the next encounter's turn start. Pre-dates the curses wave (`apex` reaches this ordering on main); curses add another self-damage source, not a new ordering. Not observed to change any sim outcome. | Low | 2026-09-10 | curses wave (PR #76), adversarial seat | If a run is ever seen to survive or win a turn it should have died on, order the player-death check before the enemy-death check in `turnStart` | OPEN |
| 12 | `candidates.candidateWords` computes each candidate's damage from `bestGold` (the theoretical maximum gold a word could pick up on the current grid) while the hit that actually lands in `submitWord` uses `selectionGold` (the gold on the tiles the player really played), so on a gold-tile grid the candidate-list / compendium number can read higher than the number the word lands. Pre-existing armour-era behaviour (the two paths have always used different gold sources); surfaced while auditing the resist preview parity (PR #86 review), which added resist on top of both paths but did not change the gold divergence. Not reachable without gold tiles on the board. | Low | 2026-09-11 | resist review (PR #86), surfaced by challenge wave | When a wave touches candidate-word scoring or gold tiles, converge the two paths on one gold source (or document the candidate number as an upper bound in the UI) | OPEN |
| 13 | `resistLabel` (reducer.ts) returns a bare `"resist"` for any Condition kind that is not word-shaped (`hpBelow`, `turnEvery`, `enemyHpBelow`, `firstTurn`, and the other state/cadence kinds), because those demands have no compact word-property label for the intent line. Harmless today: all three shipped demanders use `minLength`, which has a real label; and a non-word-shaped resist demand would be nonsensical (a word can never satisfy `firstTurn`) so none should ever be authored. | Low | 2026-09-11 | resist review (PR #86), challenge wave | If an enemy is ever authored with a non-word-shaped resist condition, either give `resistLabel` a real string for that kind or reject the condition at content-test time | OPEN |
